{
  "name": "aws-rest-with-dynamodb",
  "version": "1.0.0",
  "description": "Serverless CRUD service exposing a REST HTTP interface",
  "keywords": [
    "aws",
    "codebuild",
    "pipeline",
    "serverless",
    "todo"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/nerdguru/serverlessTodos.git"
  },
  "bugs": {
    "url": "https://github.com/nerdguru/serverlessTodos/issues"
  },
  "author": "Pete Johnson <petecj2@gmail.com>",
  "contributors": [
    "Sebastian Müller <mail@sbstjn.com> (https://sbstjn.com)"
  ],
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "test:remote": "export TODOS_ENDPOINT=$(node helpers/getEndpoint.js); npm test",
    "clean": "rm -rf node_modules && rm -rf src/node_modules",
    "deploy": "./node_modules/.bin/sls deploy | tee .build/deploy.out",
    "deploy:cicd": "./node_modules/.bin/sls deploy -s cicd | tee .build/deploy.out",
    "predeploy": "mkdir -p .build",
    "predeploy:cicd": "mkdir -p .build && npm run predeploy",
    "install:lambda": "cd src; npm install; cd ..",
    "install": "npm run install:lambda"
  },
  "devDependencies": {
    "aws-sdk": "^2.49.0",
    "babel-eslint": "^7.2.3",
    "chai": "^3.5.0",
    "eslint": "^3.19.0",
    "eslint-config-airbnb": "^14.1.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^4.0.0",
    "eslint-plugin-react": "^6.10.3",
    "mocha": "^3.3.0",
    "request": "^2.81.0",
    "request-promise": "^4.2.0",
    "serverless": "^1.12.1"
  }
}
